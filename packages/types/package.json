{
  "name": "@michi/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types and GraphQL schema for Michi",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "echo 'Run make schema to generate GraphQL types'"
  }
}
