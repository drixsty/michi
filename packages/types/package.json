{
  "name": "@michi/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types and GraphQL schema for Michi",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'Run make schema to generate GraphQL types'",
    "dev": "echo 'No dev for types yet'",
    "lint": "echo 'No lint for types yet'",
    "test": "echo 'No test for types yet'"
  }
}
