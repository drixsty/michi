{
  "name": "@michi/mcp-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn --app-dir src main:app --reload --host 0.0.0.0 --port 8002",
    "test": "pytest",
    "lint": "echo 'No lint script yet'",
    "build": "echo 'No build script yet'"
  }
}
