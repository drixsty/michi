{
  "query": "query GetFinancialOverview($storeId: ID, $channel: String) { financialOverview(storeId: $storeId, channel: $channel) { kpis { inventoryValueCost } topRisks { sku sourcePlatform } activePlatforms capitalBreakdown { platform value } message } }",
  "variables": {
    "storeId": null,
    "channel": "amazon"
  }
}
